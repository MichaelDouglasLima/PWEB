
function toMaius() {
    var novaFrase = document.getElementById("frase");

    novaFrase.value = frase.value.toUpperCase();
}

function toMinus() {
    var novaFrase = document.getElementById("frase");

    novaFrase.value = frase.value.toLowerCase();
}